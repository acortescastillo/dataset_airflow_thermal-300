# dataset_airflow_thermal-300
The dataset includes five primary variables: airflow velocity (airflow_mps), airflow distribution index (distribution_idx), rack position (rack_level), maximum temperature (max_temp_c), and hotspot occurrence (hotspots).
